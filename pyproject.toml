# SPDX-License-Identifier: MIT
[project]
name = "erpnext_mcp"
authors = [{ name = "Tim Polehn", email = "polehntim@gmail.com" }]
description = "Model Context Protocol (MCP) server for ERPNext, as a Frappe app."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
license = { text = "MIT" }
keywords = ["frappe", "erpnext", "mcp", "model-context-protocol", "ai"]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Framework :: Frappe",
]

# The MCP wire protocol is JSON-RPC 2.0 over HTTP, which this app speaks with the
# standard library — pulling in the `mcp` SDK would add an asyncio server runtime
# that a Frappe/Werkzeug (synchronous WSGI) request never uses.
#
# The two below arrived in v0.12.0 with field boundaries, and they are the only
# runtime dependencies beyond Frappe/ERPNext itself. Both do geometry this app
# has no business reimplementing: shapely decides whether a polygon is valid and
# whether a point is inside it, and h3 turns a shape into an index a bucket log
# or a crew track can be joined against.
#
# THEY ARE IMPORTED DEFENSIVELY ANYWAY. A bench that somehow lacks them loses the
# five geospatial tools — which say so by name, with the pip command to fix it —
# rather than failing to load the other hundred and sixteen. That is the same
# promise the rest of the app makes: installing it cannot break a site.
# The third arrived in v0.17.0 with the mobile login QR. `segno` is pure Python
# with no dependency of its own — which is why it and not `qrcode`, whose PNG
# writer needs Pillow. It produces only the MODULE MATRIX here; the PNG is
# written by `render/qr.py` in thirty lines of zlib, so the image an operator
# archives is byte-identical whichever encoder a bench happens to have.
#
# The fourth arrived in v0.36.0 with the tax form PDFs. `reportlab` draws a
# letter-size page of labelled boxes with the computed values in them — a W-2's
# twenty boxes, Form 941's fifteen lines, the Washington wage detail. The app's
# own `render/pdf.py` writes flowing text documents and would have had to grow a
# second layout engine to place a grid; this is the library whose whole job that
# is, and the six layouts are the only thing in the app that needs it.
#
# The fifth arrived in v0.47.1 with Form I-9. It is NOT a second reportlab and
# does the opposite job: reportlab DRAWS a page this app laid out, and `pypdf`
# FILLS one somebody else published. USCIS distributes Form I-9 as a plain-paper
# fillable PDF with 133 named AcroForm fields, every employer in the country
# completes that exact document, and it is retained rather than filed — so the
# honest output is the government's own page with values written into its own
# fields, and redrawing it would produce a lookalike nobody asked for. The
# template ships in `erpnext_mcp/templates/`; `pypdf` is what writes into it.
# v0.48.0 gave it a second form on the same argument — the IRS's Form W-4, which
# is published, completed and retained exactly the same way. Two templates in
# that directory now, one library, and no second layout engine.
#
# The sixth arrived in v0.51.0 with the stamped signature. `Pillow` does one
# job: turning the handset's signature capture into ink. That capture is a white
# rectangle with a stroke in the middle of it — `SignatureCanvas.renderPNG`
# renders opaque — so drawing it onto a federal form as-is paints a white box
# over the signature rule and everything printed around it. Pillow keys the
# paper out and crops to the stroke, and reportlab then places the result.
# Frappe itself depends on Pillow, so a bench that runs this app already has it;
# it is declared because this app now genuinely uses it rather than inheriting
# it, and a dependency you rely on and do not name is one that vanishes on the
# upgrade that drops it upstream.
#
# ALL SIX ARE IMPORTED DEFENSIVELY. A bench missing shapely or h3 loses the
# six geospatial tools; a bench missing every QR encoder loses exactly one —
# `generate_mobile_login_qr`; a bench missing reportlab loses exactly two —
# `render_tax_form_pdf` and `bulk_render_tax_form_pdfs`, with every tax form's
# computed values still readable through `get_tax_form`; a bench missing pypdf
# loses exactly two — `render_i9_pdf` and `render_w4_pdf`, with every I-9 value
# still readable through `get_i9_form`, every withholding election through
# `get_w4`, and the I-9 Print Format still printing in the Desk.
# Each says so BY NAME with the pip command to fix it, rather than failing to
# load the other three hundred. `qrcode` is accepted in segno's place where a
# bench already has it. That is the same promise the rest of the app makes:
# installing it cannot break a site.
dependencies = [
  "shapely>=2.0",
  "h3>=4.0.0",
  "segno>=1.5",
  "reportlab>=4.0",
  "pypdf>=4.0",
  "Pillow>=9.0",
  # v0.167.0. The slope aspect layer's arithmetic. Already installed wherever
  # shapely is — shapely depends on it — and imported defensively the same way.
  "numpy>=1.23",
]

[project.urls]
Homepage = "https://github.com/polehntim-commits/erpnext_mcp"
Source = "https://github.com/polehntim-commits/erpnext_mcp"

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "erpnext_mcp"

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = ["F", "E", "W", "I", "UP", "B", "RUF"]
ignore = [
  "B017", # assertRaises(Exception)
  "B023", # function definition does not bind loop variable
  "E101", # indentation contains mixed spaces and tabs
  "E501", # line too long
  "F403", # can't detect undefined names from star imports
  "W191", # indentation contains tabs
  # The three below flag non-ASCII characters that "look like" ASCII ones. Every
  # occurrence in this app is deliberate typography in prose an operator reads:
  # an EN DASH in a numeric or regulation range (`1–30`, `§112.21–.30`), a
  # MULTIPLICATION SIGN in a dimension or a count (`8.5×11`, `600 × 800 KB`),
  # and a MINUS SIGN in arithmetic worked out in a comment. Rewriting them to
  # hyphens and lowercase `x` would make the text worse, and a `noqa` on each of
  # the sixteen would be more noise than the rules are worth.
  "RUF001", # ambiguous unicode character in a string
  "RUF002", # ambiguous unicode character in a docstring
  "RUF003", # ambiguous unicode character in a comment
]

# UP031 wants `%`-formatting rewritten to a format specifier. In these ten
# modules the `%`-string is not a string being formatted at the point it is
# written — it is a CSS/JS TEMPLATE CONSTANT held at module level and filled in
# much later with a `% {...}` mapping. Two things follow. The substitution is
# deferred, so an f-string cannot express it at all; and the template body is
# dense with the literal `{` and `}` of CSS rules and JS blocks, so `.format()`
# would require doubling every brace in a stylesheet — dozens per file — which
# turns readable CSS into something nobody will edit correctly twice.
# `pdf_signing` is the same judgement for a different reason: its `%.5f` runs
# are PDF content-stream operators, and the PDF spec itself is written that way.
# The rule is right about ordinary strings and stays on everywhere else.
[tool.ruff.lint.per-file-ignores]
"erpnext_mcp/asset_tag_form_action.py" = ["UP031"]
"erpnext_mcp/asset_tag_list_action.py" = ["UP031"]
"erpnext_mcp/asset_tag_sheet.py" = ["UP031"]
"erpnext_mcp/badge_form_action.py" = ["UP031"]
"erpnext_mcp/badge_list_action.py" = ["UP031"]
"erpnext_mcp/badge_sheet.py" = ["UP031"]
"erpnext_mcp/farm_task_map_action.py" = ["UP031"]
"erpnext_mcp/mobile_onboarding.py" = ["UP031"]
"erpnext_mcp/onboard_worker_action.py" = ["UP031"]
"erpnext_mcp/pdf_signing.py" = ["UP031"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
# The formatter reaches into ```python blocks inside Markdown, and every
# Markdown file here that holds one is a RECORD OF CODE THAT USED TO EXIST:
# RELEASES/v0.35.0.md quotes a dict with a duplicated key and an arrow comment
# pointing at the entry the duplicate silently ate, and
# docs/reviews/cross_system_review_2026_08_07.md column-aligns trailing
# `# hr.py:286` references so they read down the page as a list of sites.
# Formatting either one rewrites a quotation to match a style the quoted code
# never had — it moved the arrow off the line it points at and collapsed the
# alignment — which is worse than useless in a document whose whole job is to
# say what was there. Prose is excluded from the FORMATTER only; `ruff check`
# still reads these files.
exclude = ["*.md"]
